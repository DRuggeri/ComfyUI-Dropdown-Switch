[project]
name = "comfyui-dropdown-switch"
version = "1.0.0"
description = "A frontend-only ComfyUI node that routes one of several inputs to the output based on a dropdown selection."
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://github.com/YOUR_USERNAME/ComfyUI-Dropdown-Switch"

[tool.comfy]
PublisherId = ""
DisplayName = "Dropdown Switch"
Icon = ""
